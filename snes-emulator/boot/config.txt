# Default settings
arm_64bit=0
enable_uart=1
initial_turbo=60
disable_splash=1
hdmi_drive=2

# Force Full HD (1080p 60Hz) output
hdmi_group=2
hdmi_mode=82

# Center the 4:3 framebuffer with black pillars on a 16:9 screen
disable_overscan=0
overscan_left=240
overscan_right=240
overscan_top=0
overscan_bottom=0

[pi3]
kernel=kernel8-32.img
dtoverlay=pi3-disable-bt
arm_freq=1200
force_turbo=1

[pi4]
kernel=kernel7l.img
armstub=armstub7-rpi4.bin
max_framebuffers=2

[cm4]
otg_mode=1

# Note for Debugging & Performance:
# To customize the application at boot, you can create a file named `cmdline.txt`
# on the root of your SD card (single line with space-delimited options).
#
# Recommended content for high performance (uses screen for logs):
#   fast=true logdev=tty1 loglevel=3
#
# To debug via the serial interface (GPIO 14 & 15 at 115200 baud):
#   fast=true logdev=ttyS1 loglevel=4

